<p id="notice"><%= notice %></p>

<div class="header_product">
  <h1>Products</h1>
</div>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Image url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td>Organic Cotton Dress<%= product.name %></td>
        <td>Great eco-friendly dress<%= product.description %></td>
        <td>StripeDress.jpg<%= product.image_url %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>  
  <tbody>
    <% @products.each do |product| %>
      <tr>
        <td>Organic Cotton Sweater<%= product.name %></td>
        <td>Great eco-friendly sweater<%= product.description %></td>
        <td>YellowSweater.jpg<%= product.image_url %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
  <tbody>
    <% @products.each do |product| %>
      <tr> 
        <td>Organic Cotton Top<% product.name %></td>
        <td>Great eco-friendly top<%= product.description %></td>
        <td>WhiteTop.jpg<%= product.image_url %></td>
        <td><%= link_to 'Show', product %></td>
        <td><%= link_to 'Edit', edit_product_path(product) %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>  
  </tbody>
</table>

<br>

<%= link_to 'New Product', new_product_path %>





